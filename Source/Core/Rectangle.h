#pragma once

namespace Hydro
{
    
    /**
     * \brief Describes a rect area with (x, y) position and extend (size). Should contains pixel coordinate values
     */
    struct Rectangle
    {
        float X, Y, Width, Height;
    };
}
